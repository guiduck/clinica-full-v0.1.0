DATABASE_URL="postgresql://clinica:clinica@localhost:5433/clinica_agil?schema=public"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_API_BASE_URL="http://localhost:3000"
SESSION_COOKIE_NAME="clinica_session"
SESSION_SECRET="change-me-in-local-env"
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_WHATSAPP_FROM="whatsapp:+00000000000"

# Card data safety: never persist raw card number or CVV in the app.
# Store only a provider-safe token/reference and non-sensitive display metadata.
